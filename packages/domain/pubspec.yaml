name: domain
description: >-
  Núcleo de dominio (Dart puro): entidades, Money, SplitEngine y BalanceEngine.
  PROHIBIDO importar Flutter o Firebase en este paquete.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
