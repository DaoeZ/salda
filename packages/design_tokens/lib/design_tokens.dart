/// Tokens de diseño generados desde la fuente única
/// (`assets/brand.json` + `assets/design_tokens.json`).
///
/// Para regenerar tras editar los JSON: `dart run design_tokens:generate`.
library;

export 'src/tokens.g.dart';
