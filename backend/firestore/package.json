{
  "name": "firestore-rules-tests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tests de la matriz de autorización (spec §13.2) contra el Emulator Suite. Ejecutar con: firebase emulators:exec --only firestore --project demo-salda \"npm --prefix backend/firestore test\" (desde la raíz).",
  "scripts": {
    "test": "node --test --test-concurrency=1 \"test/**/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
