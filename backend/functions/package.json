{
  "name": "functions",
  "private": true,
  "version": "0.1.0",
  "description": "Cloud Functions v2 de Salda: recompute (calculadora autoritativa), notify (FCM) y cleanup (borrado en cascada). Ver ESPECIFICACION.md §12.2.",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "npm run build && node --test \"lib/test/**/*.test.js\" --test-skip-pattern=\"\\.it\\.\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test:integration": "npm run build && node --test \"lib/test/integration/**/*.it.test.js\""
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
