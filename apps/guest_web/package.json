{
  "name": "guest-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Web de invitados de Salda (nombre provisional). SPA ligera sin lógica de dinero: pinta agregados calculados por la Cloud Function.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "firebase": "^12.0.0"
  }
}
